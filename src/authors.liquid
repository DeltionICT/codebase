---
templateEngineOverride: liquid, md
layout: layout
pagination:
   data: authors
   size: 1
   alias: author
permalink: "/authors/{{author.key}}/"
eleventyComputed:
   title: "{{author.name}}"
---

# {{ author.name }}

* Website: <a href="{{author.website}}" target="_new">{{author.website}}</a><br/>


{{ author.photo | image: 'Foto van de auteur', 10 }}

## Opdrachten

{% assign posts = collections.all | getPostsByAuthor: author.key %}
<ul>
{% for post in posts %}
	<li><a href="{{post.url}}">{{post.data.title}}</a></li>
{% endfor %}
</ul>