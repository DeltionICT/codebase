---
layout: modules_aside.njk
title: Startpagina
difficulty: basic
date: 2023-01-10
pagination:
    data: mods
    size: 1
    alias: module
permalink: "/mods/{{ module.key | slug }}/index.html"
author: jsiewers
---

<h1>{{ module.name }}</h1>
<h3>Omschrijving</h3>
<div class="description">
    {{ module.description }}
</div>
<h3>Informatie over deze module</h3>
<ul>
    <li>Studiebelasting: {{ module.sbu }} ( ± {{ (module.sbu)/24 }} weken)</li>
    {%  set evcs =  module.evc | toArray() | getObj(mods) %}
    {% if evcs | length > 0  %}  
    <li>
        Voorkennis
        <ul>
            {% for c in evcs %}
                <li>{{ c.name }}</li>
            {% endfor %}
        </ul>      
    </li>
    {% else %}
    <li>Geen voorkennis nodig</li>
    {% endif %}

    </li>
    {%  set subs =  module.subjects | toArray() | getObj(technologies) %}
    {% if subs | length > 0  %}  
    <li>
        Onderwerpen
        <ul>
            {% for s in subs %}
                <li>{{ s.name }}</li>
            {% endfor %}
        </ul>
    </li>
    {% endif %}
    {%  set cs =  module.crebos | toArray() | getObj(crebos) %}
    {% if cs | length > 0  %}  
    <li>
        Opleidingen
        <ul>
            {% for c in cs %}
                <li>{{ c.key }} - {{ c.description }}</li>
            {% endfor %}
        </ul>      
    </li>
    {% else %}
    <li>Basisdeel opleiding Software development</li>
    {% endif %}
</ul>
<h3>Links</h3>
<ul>
    <li><a href="{{ module.sbb }}">Informatie over deze kwalificatie op s-bb.nl</a></li>
    <li><a href="{{ module.linkedinlearning }}">Lesmateriaal op Linkedin Learning</a></li>
</ul>
